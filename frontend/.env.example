# URL de base du backend FastAPI (routes montées sous /api/v1).
# Doit partager le hostname avec l'URL d'accès du front (tout 127.0.0.1
# ou tout localhost) — sinon le cookie de session SameSite=Lax est bloqué.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8020/api/v1
